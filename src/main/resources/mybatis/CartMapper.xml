<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.deliveryfood.mapper.CartMapper">

    <select id="findCart" resultType="com.deliveryfood.dto.CartMenuDto">
        SELECT *
        FROM CART
        WHERE user_Id = #{userId}
    </select>

    <delete id="deleteCart">
        DELETE
        FROM CART
        WHERE FIND_IN_SET(user_Id, #{userId})
    </delete>

    <insert id="addMenu" parameterType="com.deliveryfood.dto.CartMenuDto">
        INSERT INTO CART(user_id, index, menu_id, count, reg_dt)
        VALUES(#{userId}, #{index}, #{menuId}, #{count}, #{regDt})
    </insert>

    <delete id="deleteMenu" parameterType="hashmap">
        DELETE
        FROM CART
        WHERE user_Id = #{userId}
        AND index = #{index}
    </delete>

    <insert id="insertPost">
        <selectKey keyProperty="hierarchyId" order="BEFORE" resultType="String">
            select to62(seq4post.nextval) from dual
        </selectKey>
        insert into t_post (hierarchy_id, bb_id, writer_id, post_type, title, content)
        values(#{hierarchyId}, #{board.id}, #{writer.id}, 'POST', #{title}, #{content})
    </insert>

    <insert id="insertReply">
        <selectKey keyProperty="hierarchyId" order="BEFORE" resultType="String">
            select #{parent.hierarchyId} || #{hidDeli} || to62(seq4post.nextval) from dual
        </selectKey>
        insert into t_post(hierarchy_id, writer_id, content)
        values(#{hierarchyId}, #{writer.id}, #{content})
    </insert>

</mapper>