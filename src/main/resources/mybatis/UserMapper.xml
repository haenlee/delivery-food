<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.deliveryfood.mapper.UserMapper">

    <select id="findById" resultType="com.deliveryfood.dto.UserDto">
        SELECT *
        FROM USER
        WHERE email = #{email}
    </select>

    <insert id="register" parameterType="com.deliveryfood.dto.UserDto">
        INSERT INTO USER(user_id, name , email, password, phone, address, status, reg_dt)
        VALUES(#{userId}, #{name}, #{email}, #{password}, #{phone}, #{address}, #{status}, #{regDt})
    </insert>

    <update id="withdraw" parameterType="com.deliveryfood.dto.UserDto">
        UPDATE USER
        SET status = #{status},
            udt_dt = NOW()
        WHERE user_id = #{userId}
    </update>


</mapper>